version: '3.2'

services:

  template_bot:
    container_name: template_bot
    restart: always
    image: bomzheg/karmabot
    volumes:
      - type: "bind"
        source: "${HOME}/TemplateBot/log/karmabot/"
        target: "/log"
      - type: "bind"
        source: "./config"
        target: "/config"
    env_file: config/.env
    entrypoint: [ "python3", "-m", "app"]
    networks:
      - reverse-proxy


networks:
  reverse-proxy:
    external:
      name: nginx-revers-proxy
